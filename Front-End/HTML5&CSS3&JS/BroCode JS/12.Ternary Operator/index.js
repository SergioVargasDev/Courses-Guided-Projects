let age = 15;

let answer = age >= 20 ? "You´re on the age" : "You´re not in the age";
console.log(answer);

price = 125;

discount = price >= 100 ? price - (price * 10) / 100 : 0;
console.log(discount);
